config networking_stack
  prompt "Include networking stack"
  bool
  default y

config networking_dhcp
  prompt "Include DHCP support"
  depends on networking_stack
  bool
  default y

config networking_dhcp6
  prompt "Include DHCPv6 client support (requires IPv6, conf-INET6=1)"
  depends on networking_stack
  bool
  default y

