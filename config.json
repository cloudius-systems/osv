{
   "modules": {
        "mgmt": {
            "type": "direct-dir",
            "path": "${OSV_BASE}/mgmt/"
        },
    "repositories": [
        "${OSV_BASE}/apps",
        "${OSV_BASE}/modules"
    ]
   }
}
